ezplatform:
    system:
        default:
            image_variations:
                ride_list:
                    reference: null
                    filters:
                        - {name: geometry/scaledownonly, params: [140, 100]}
                game_list:
                    reference: null
                    filters:
                        - {name: geometry/scaledownonly, params: [140, 100]}
